use crate::solvers::{Solver, SolverTrait};

impl SolverTrait for Solver<2022, 23, 1> {
    fn solve(&self, _input: &str) -> impl ToString {
        ""
    }
}

impl SolverTrait for Solver<2022, 23, 2> {
    fn solve(&self, _input: &str) -> impl ToString {
        ""
    }
}
