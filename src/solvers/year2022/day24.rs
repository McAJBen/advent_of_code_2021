use crate::solvers::{Solver, SolverTrait};

impl SolverTrait<u64> for Solver<2022, 24, 1> {
    fn solve(&self, _input: &str) -> u64 {
        todo!()
    }
}

impl SolverTrait<u64> for Solver<2022, 24, 2> {
    fn solve(&self, _input: &str) -> u64 {
        todo!()
    }
}
